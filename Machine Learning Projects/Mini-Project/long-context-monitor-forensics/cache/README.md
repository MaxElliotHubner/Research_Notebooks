# Activation cache

This folder is deliberately empty in the distributed lab. Store only the
segment-boundary activations needed by the analysis, together with a small JSON
metadata file recording the model, SAE, hook, tokenizer, dtype, split, and
configuration. Do not commit model weights or full-token activation caches.
